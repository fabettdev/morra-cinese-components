import { StyleSheet } from "react-native";

const moveIconStyle = StyleSheet.create({
  wrapper: {
    borderRadius: 100,
  },
  moveImage: {
    borderRadius: 100,
  },
});

export default moveIconStyle;
